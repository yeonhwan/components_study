import { forwardRef, useEffect } from "react";

//^ only watch focus | blur events happening
//^ fires evthandler

export const Dismissable = forwardRef((props, forwardRef) => {});

//^ captures focus | blur events of children
const FocusTrap = forwardRef((props, forwardRef) => {});
